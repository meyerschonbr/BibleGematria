info:
  name: Search
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:58227/api/search
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
