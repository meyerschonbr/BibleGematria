<Window x:Class="BibleGematria.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BibleGematria.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Bible Gematria Search" Height="700" Width="1200">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>

    <!-- DataContext set in code-behind (avoids designer bug) -->
   <!-- <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext> -->

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input Section -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
            <Label Content="Hebrew Name:" FontSize="16" VerticalAlignment="Center"/>
            <TextBox x:Name="HebrewInputTextBox" 
                     Text="{Binding HebrewInput, UpdateSourceTrigger=PropertyChanged}" 
                     Width="280" Margin="12,0" FontSize="22" FontFamily="David"
                     VerticalContentAlignment="Center" VerticalAlignment="Center"
                     PreviewTextInput="HebrewInputTextBox_PreviewTextInput"/>
            <Button Content="🔍 Search" Command="{Binding SearchCommand}"
                    Padding="25,12" Margin="12,0" FontSize="16" MinWidth="110"/>
            <TextBlock Text="Gematria:" Margin="20,0,6,0" FontSize="16" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding GematriaValue}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="Searching..." Margin="20,0" FontSize="16" Foreground="DodgerBlue" VerticalAlignment="Center"
                       Visibility="{Binding IsSearching, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
                <TextBlock Text="Books:" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <CheckBox Content="All"
                  IsChecked="{Binding AllBooksSelected, Mode=TwoWay}"
                  Margin="0,0,12,0"
                  VerticalAlignment="Center"/>
                <CheckBox Content="Don't cross commas"
                  IsChecked="{Binding NoCrossEtnachta, Mode=TwoWay}"
                  Margin="20,0,0,0"
                  VerticalAlignment="Center"
                  ToolTip="Only return multi-word matches that stay within a single clause (do not cross an etnachta boundary)"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- תורה -->
                <GroupBox Grid.Column="0" Header="תורה" Margin="0,0,10,0" Padding="8">
                    <ItemsControl ItemsSource="{Binding TorahBooks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Margin="0,2,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- נביאים -->
                <GroupBox Grid.Column="1" Header="נביאים" Margin="0,0,10,0" Padding="8">
                    <ItemsControl ItemsSource="{Binding ProphetsBooks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding DisplayName}"
                          IsChecked="{Binding IsSelected, Mode=TwoWay}"
                          Margin="0,2,12,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- כתובים -->
                <GroupBox Grid.Column="2" Header="כתובים" Padding="8">
                    <ItemsControl ItemsSource="{Binding WritingsBooks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding DisplayName}"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                  Margin="0,2,0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>
            </Grid>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Matches found:" FontSize="14" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ResultCount}" Margin="6,0,0,0" FontSize="14"/>

            <TextBlock Text="No results found."
               Margin="20,0,0,0"
               FontSize="14"
               Foreground="Firebrick"
               Visibility="{Binding ShowNoResultsMessage, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Results Grid -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Results}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  CanUserReorderColumns="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Book"
                        Binding="{Binding BookName}"
                        Width="85"
                        FontWeight="SemiBold" />

                <DataGridTextColumn Header="ChapterVerse" Width="90">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}:{1}">
                            <Binding Path="Chapter" />
                            <Binding Path="VerseNumber" />
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Match"
                        Binding="{Binding MatchedText}"
                        Width="160"
                        FontWeight="Bold" />

                <DataGridTemplateColumn Header="Full Verse" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock TextWrapping="Wrap"
                                       DataContextChanged="VerseTextBlock_DataContextChanged"
                                       FlowDirection="RightToLeft"
                                       TextAlignment="Right" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

